apiVersion: 1

providers:
  - name: V14
    orgId: 1
    folder: V14
    folderUid: v14
    type: file
    disableDeletion: true
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: ${GRAFANA_DASHBOARDS_PATH}
